{
    "version": "9.8.2",
    "description": "NoMachine. A remote-control tool, available for multiple platforms",
    "homepage": "https://www.nomachine.com/",
    "license": "Proprietary",
    "changelog": "https://www.nomachine.com/what-s-new-in-nomachine",
    "architecture": {
        "64bit": {
            "url": "https://download.nomachine.com/download/9.8/Windows/nomachine_9.8.2_1_x64.exe#/setup.exe",
            "hash": "c22415f3eae312e24a78a622cf126d0e79be0581102dbd2f6679d19dd29c49d2"
        }
    },
    "pre_download": "Assert-Administrator",
    "pre_install": [
        "$processname = \"NoMachine\"",
        "$process = Get-Process $processname -ErrorAction SilentlyContinue",
        "if ($process) {",
        "   $process.CloseMainWindow()",
        "   Start-Sleep -Seconds 5",
        "   if (!$process.HasExited) {",
        "       Start-Sleep -Seconds 25",
        "       $process | Stop-Process -Force",
        "   }",
        "}"
    ],
    "installer": {
        "script": [
            "$programname = \"NoMachine*\"",
            "$ProgramFiles = [Environment]::GetFolderPath(\"ProgramFilesX86\")",
            "$filepath = \"$ProgramFiles\\NoMachine\\bin\\nxclient.exe\"",
            "if(((Test-Path -Path $filepath -PathType Leaf) -eq $false) -or ((Get-Command $filepath).Version -lt [System.Version]\"$version\"))",
            "{",
            "   &\"$dir\\setup.exe\" /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP- | Out-Null",
            "   if(((Test-Path -Path $filepath -PathType Leaf) -eq $false) -or ((Get-Command $filepath).Version -lt [System.Version]\"$version\"))",
            "   {",
            "       exit 1",
            "   }",
            "}",
            "if (Test-Path -Path \"$dir\\setup.exe\") { Remove-Item \"$dir\\setup.exe\" -Force }"
        ]
    },
    "post_install": [
        "$programname = \"NoMachine\"",
        "$CommonDesktopPath = [Environment]::GetFolderPath(\"CommonDesktopDirectory\")",
        "if (Test-Path \"$CommonDesktopPath\\$programname.lnk\") {Remove-Item \"$CommonDesktopPath\\$programname.lnk\" -Force}"
    ],
    "checkver": {
        "url": "https://download.nomachine.com/download/?id=3&platform=windows",
        "regex": "<div class=\"d-table-cell info_cell\"><p>([\\d.]+)_(?<revision>[\\d]+)"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://download.nomachine.com/download/$majorVersion.$minorVersion/Windows/nomachine_$version_$matchRevision_x64.exe#/setup.exe"
            }
        }
    }
}
